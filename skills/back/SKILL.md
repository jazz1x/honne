---
name: back
version: 0.0.1
description: >
  Alias for honne:compare. Read-only retrospective — reads as "honne back".
  Triggers: "honne back", "look back", "compare runs", "지난번이랑 비교".
---

Alias for `honne:compare`. Execute the `compare` skill now with the same context and arguments.
